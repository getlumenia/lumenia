# pin pnpm settings so CI (no user ~/.npmrc) matches the lockfile's settings.autoInstallPeers
auto-install-peers=false

# JSR packages through the npm-compat registry (pnpm 9 has no jsr: specifier).
@jsr:registry=https://npm.jsr.io
